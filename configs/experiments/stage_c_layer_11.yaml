# Stage C: Shared MoE placed on layer 11 only
base_config: ../vihsd.yaml

model:
  architecture: phobert_moe
  moe_layers: [11]
  num_experts: 4
  top_k: 1
  expert_type: gelu
  upcycle: true
  shared_expert: true

training:
  loss_type: cross_entropy

routing:
  load_balance_loss_factor: 0.01
