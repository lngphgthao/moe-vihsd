# Configuration

`vihsd.yaml` controls dataset columns and splits, tokenizer settings, model dimensions, routing (`num_experts`, `top_k`), load-balancing loss, training hyperparameters, W&B, and output paths.
