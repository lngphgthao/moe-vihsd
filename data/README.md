# Data Storage Directory

This directory is reserved for local dataset caches, raw splits, and temporary downloads (ignored by git per `.gitignore`).
Data preparation and tokenization logic is located in [`src/dataset.py`](../src/dataset.py).
